This is the frontend portion of the Dead Maze tracker site at https://projects.fewfre.com/a801/deadmaze/tracker/

# Technical

This project uses a Svelte (vanilla svelte, not SvelteKit) with Typescript frontend with Vite for building

As per the repo name this is only the frontend, as I don't want to expose the backend for easier manipulation by trolls